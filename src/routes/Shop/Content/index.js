import { hot } from 'react-hot-loader/root';

import ShopContent from 'components/organisms/ShopContent ';

import PageLayout from 'layouts/Page';

export default hot(PageLayout({ Content: ShopContent }));
